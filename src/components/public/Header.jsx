import {useEffect,useState} from 'react';
const links=[['Início','inicio'],['O evento','evento'],['Confirmar presença','confirmar'],['Lista de presentes','presentes']];
const goTo=id=>document.getElementById(id)?.scrollIntoView({behavior:'smooth',block:'start'});
export default function Header(){const [open,setOpen]=useState(false);useEffect(()=>{const f=e=>e.key==='Escape'&&setOpen(false);document.addEventListener('keydown',f);return()=>document.removeEventListener('keydown',f)},[]);const navigate=id=>{setOpen(false);goTo(id)};return <header className="header"><button className="brand brand-button" onClick={()=>navigate('inicio')} aria-label="Ir para o início"><span>B</span> baby shower</button><button className="menu-btn" onClick={()=>setOpen(!open)} aria-expanded={open} aria-label="Abrir menu">{open?'×':'☰'}</button><nav className={open?'nav open':'nav'} aria-label="Navegação principal">{links.map(([label,id])=><button key={id} onClick={()=>navigate(id)}>{label}</button>)}</nav></header>}
